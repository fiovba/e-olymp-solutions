#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x1, y1, x2, y2, z2;
    double d;

    cin>>x1>>y1;
    cin>>x2>>y2>>z2;

    d=sqrt(pow(x1-x2,2)+pow(y1-y2,2)+pow(z2,2));
    if (d!=0) 
        d=1/d;

   printf("%.3lf",d);

    return 0;
}
