#include<stdlib.h>
#include <stdio.h>

int main() {
   long long n,b=0;
   scanf("%lld",&n);
   if(n==0)
   n=1;
   for(int i=10;n>=1;i*10){
       n/=i;
       b++;
       
   }
   printf("%lld",b);
    return 0;
}
